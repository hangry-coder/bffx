package router

// AI pipeline route modules (Phase 6 decomposition):
//   - pipelines_helpers.go — catalog resolution, media ingest helpers, quota checks
//   - pipelines_routes.go  — chatbot (SSE) and ingestion pipeline route registration
